import { FC } from 'react'
import { Button } from '@components/ui/button'

const MoveActionButton:FC = () => {
    return (
        <Button variant='outline' className='text-sm'>Move</Button>
    )
}

export default MoveActionButton